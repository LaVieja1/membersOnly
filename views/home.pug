extends layout 

block content 
    div.post_container
        for post in posts 
            div.post_card
                div.date #{post.timestamp_formatted}
                div.title!= post.title
                hr
                div.content!= post.text